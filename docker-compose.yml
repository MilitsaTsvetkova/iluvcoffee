services:
  db-nestjs:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: nestjs_user
      POSTGRES_DB: nestjs_db
      POSTGRES_PASSWORD: Password123
    ports:
      - '5432:5432'
